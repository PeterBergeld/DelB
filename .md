## Nu ska vi lägga upp uppgiften

Det är viktigt för mig att förstå koden och konvertera den fram och tillbaka.
Kod till uppgift och uppgift till kod.

.gitignore läggs till

 /bin  
 /obj

 ## In Courses

 public string? Name {get; set} // vi har ett fält med name som vi ska kunna get och set från obj. // kolla upp närmare va set i praktiken betyder i koden 
 Vi har alltså skapat en lista som ska ta från students

 Maxseats löser mängden plats kapacitet som klassen har = public int MaxSeats {get ; det;}
 (kommer vara ett heltal i och med int)

## .Add, if satser
public List<Student> Students {get; set;} = new List<Student>(); 
Säger oss att en Course kan hålla flera Student-object

Enroll(student)
Med olika if satser som 
ställer villkor.
Angående om studenten är redan anmäld
if Students.Count >= MaxSeats
om students is more than Maxseats "The course is full"


 if this object Contains in Corses and its student
// Vi får läsa höger till vänster
!student.Courses.Cointains(this))
and this goes under add
Students.Add(student);

return true; 

Inga dubbletter

Kapacitet

Lägg till studenten i kursen

And student.Courses.Add(this);

if (!student.Courses.Contains(this))

public void Remove == ta bort en studerande ur kursen . av returntype. Ska inte returnera ett värde


RollCall() är metoden som skriver ut alla studerande i kursen

for each == för varje student  i classen Student ska name skrivas ut 

public override string ToString. Här blir koden spännande nu "override:ar" vi superclass and the subclass implments a specific method that has been provided by the
superclass
Animal --->Dog example (Subclass)



## Student.cs

Fält becomes: public string Name {get;set}

Create a List<Course> Course {get;set}= new List<Course>();
To "grab" the students from the courses

Join(course)

public void Join(Course course)

{
     
     course.Enroll(this)
}

Same story here. From right to left: The method nanmed course takes from the class Course. We take this and enroll it into course
The enroll function is already checking from both sides (Both Student and Course)

Leave(course)
if the student leaves we go into
if (Courses.Contain(course))
Remove (this (Name)) from courses == 
Ta bort this from course.
And this updates both lists.

Schedule()
public void Schedule()
skriver ut vilkaa kurser studenten går. 
Console.WriteLine(course);
(Then C# use the Course.Tostring().
To add the students in the corresponding courses.)

Students ToString()
and override string ToString()
 {
     return Name ?? "";
}
From the subclass we override the method given from the superclass



## Program.cs


Here we add the courses
Add the students
student.Join(course);
Rollcall() to show the students
Shows the schedule of the students
The duplicate enrollment stops
Test full course
And leaving the course.

To enroll direclty 

Console.WriteLine("\nAnna går med i Programmering direkt:");
programming.Enroll(student2);

Show Annas schedule

remove directly from course 

and lastly shwo Anna`s schdule again. 




### __End of excersise__
     -----------------











































